apiVersion: v1
kind: Service
metadata:
  name: ecomm-frontend
  namespace: ecomm-staging
spec:
  selector:
    app: ecomm-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30100
  type: NodePort
