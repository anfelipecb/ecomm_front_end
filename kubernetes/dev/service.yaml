apiVersion: v1
kind: Service
metadata:
  name: ecomm-frontend
  namespace: ecomm-dev
spec:
  selector:
    app: ecomm-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30000
  type: NodePort
